interface:
  display_name: "Xcode MCP Workflow"
  short_description: "Prefer Xcode MCP for Xcode project work"
  default_prompt: "Use $xcode-mcp-workflow to work on this Xcode project with MCP-first navigation, file operations, builds, tests, diagnostics, previews, and Swift snippet execution."

dependencies:
  tools:
    - type: "mcp"
      value: "xcode"
      description: "Xcode MCP server for project navigation, file operations, builds, tests, diagnostics, previews, and snippets"

policy:
  allow_implicit_invocation: true
